# RL training dependencies for deckgym-core
# Install with: pip install -r requirements.txt
# Note: PyTorch should be installed separately for CUDA support:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# Core RL
stable-baselines3[extra]>=2.7.0
sb3-contrib>=2.7.0
gymnasium>=1.0.0

# Deep learning
numpy>=2.0.0

# Monitoring
tensorboard>=2.18.0

# Rust bindings (build from source)
maturin>=1.5.0
